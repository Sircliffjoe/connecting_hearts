<div class="max-w-4xl mx-auto space-y-6">
  <div class="flex items-center justify-between border-b border-slate-800 pb-4">
    <h1 class="font-serif text-2xl font-bold text-white">Edit Resource: <%= @resource.title %></h1>
    <%= link_to "← Back to Resources", admin_resources_path, class: "text-xs text-slate-400 hover:text-white transition-colors" %>
  </div>

  <div class="bg-slate-900 border border-slate-800 rounded-2xl p-6 md:p-8">
    <%= render "form", resource: @resource %>
  </div>
</div>
